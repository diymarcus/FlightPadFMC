plugins {
    id 'com.android.application'
}

android {
    namespace 'com.virtualfmc.fbwa320'
    compileSdk 34

    defaultConfig {
        applicationId "com.virtualfmc.fbwa320"
        minSdk 19
        targetSdk 34
        versionCode 3
        versionName "0.4.0"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
        debug {
            minifyEnabled false
        }
    }

    // AGP 8+ requires explicit opt-in to BuildConfig generation. We read
    // BuildConfig.VERSION_NAME in MainActivity so the identify message can
    // tell the server which app version is connecting (drives the "Android
    // app outdated" warning in the Server Console).
    buildFeatures {
        buildConfig = true
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_11
        targetCompatibility JavaVersion.VERSION_11
    }

    applicationVariants.all { variant ->
        variant.outputs.all {
            outputFileName = "FBWA320.apk"
        }
    }
}

dependencies {
    // Android Support Library (for API 19 compatibility)
    implementation 'com.android.support:appcompat-v7:28.0.0'
    implementation 'com.android.support:support-compat:28.0.0'

    // WebSocket client (nv-websocket-client)
    implementation 'com.neovisionaries:nv-websocket-client:2.14'

    // JSON handling: use Android's built-in org.json.* — no Maven dependency
    // (org.json:json removed — conflicts with Android platform classes)

    // Testing
    testImplementation 'junit:junit:4.13.2'
    androidTestImplementation 'com.android.support.test:runner:1.0.2'
    androidTestImplementation 'com.android.support.test.espresso:espresso-core:3.0.2'
}
