# ── Keystore / signing secrets — NEVER commit ────────────────────────────
KeyStore/
*.jks
*.keystore
keystore.properties

# ── Android build artifacts — all 5 app modules ──────────────────────────
FMC777/build/
FMC777/app/build/
FMC737/build/
FMC737/app/build/
FBWA320/build/
FBWA320/app/build/
FMC777_OWN/build/
FMC777_OWN/app/build/
FenixA320/build/
FenixA320/app/build/

# ── Signed APK output — published with each release tag
#    (FMC777_OWN stays excluded via the FMC777_OWN/ blanket rule below)

# ── FBWA320 working folder (skins, calibration notes, personal materials) ─
FBWA320/CDUPIC/

# ── Personal cockpit variant — private, do not publish ───────────────────
FMC777_OWN/

# ── iFly 737 MAX private dev notes (PLAN.md, CALIBRATION.md, raw SDK
#    cross-reference) — Android sources publish from v0.4.0 onward, but
#    the in-tree dev artefacts stay private. PMDG headers inside iFly737MAX/
#    are caught by the global *.h rule below.
iFly737MAX/PLAN.md
iFly737MAX/CALIBRATION.md

# ── FenixA320 private dev notes (never publish)
FenixA320/Keys.txt
FenixA320/PLAN.md

# ── Android / Gradle / IDE local state ───────────────────────────────────
*.iml
.gradle/
.idea/
local.properties
.claude/

# ── Server source — distributed as .exe only, source stays private ──────
server_gui.py
server_gui_backup.py
server_gui_old.py
server_gui_test.py
server_gui_backup_*.py
fenix_handler.py
ifly737max_handler.py
compute_ng3_offsets.py
FlightPadFMCServer.spec

# ── Server GUI image assets — packaged into the .exe, not for repo ──────
Donate.jpeg
ico4.ico
Logo.jpg
LogoMA.jpg
LogoMA_Sil.jpg
LogoMA_White.jpg
Server.jpg
ServerS.jpg
Youtube_logo.jpg
Youtube_logo.webp

# ── Internal planning / release-prep docs (never publish) ───────────────
AIRCRAFT_DETECTION_PLAN.md
community_post_*.md

# ── Private cert material ───────────────────────────────────────────────
Cert/

# ── Python build output ─────────────────────────────────────────────────
__pycache__/
*.pyc
build/
*.spec

# ── dist/ — publish FlightPadFMCServer.exe + SimConnect DLLs only
#    Exclude per-user runtime state (logs + settings) from the repo
dist/FlightPadFMC_log.txt
dist/FlightPadFMC_log_old.txt
dist/settings.ini

# ── External SDKs / upstream project clones (not ours to redistribute) ──
WASM/
iFly737MAX_SDK/

# ── PMDG / iFly copyrighted SDK headers (must not redistribute) ─────────
*.h
PMDG_*_EVENT_IDs.TXT

# ── Personal notes / dev artifacts / old copies ─────────────────────────
Notes.txt
Project_old.md
CDU_Images/
Product/
Test/
Wbsite.txt
Server_Console.jpg
tools/

# ── Logs ────────────────────────────────────────────────────────────────
FlightPadFMC_log.txt
FlightPadFMC_log_old.txt
